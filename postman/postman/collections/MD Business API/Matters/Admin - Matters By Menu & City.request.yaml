$kind: http-request
url: "{{base_url}}/matters/admin/:menu/:city/all-matters"
method: GET
pathVariables:
  menu: "1"
  city: "1"
auth:
  type: bearer
  credentials:
    token: "{{admin_token}}"
order: 6000
