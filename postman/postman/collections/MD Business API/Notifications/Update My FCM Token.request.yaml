$kind: http-request
url: "{{base_url}}/notifications/token"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "fcm_key": "device-fcm-token"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
